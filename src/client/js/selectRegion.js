import { CallbackProperty, defined, Rectangle, Cartesian3, Math, VerticalOrigin, HeightReference, NearFarScalar, Color, Billboard } from "cesium";
import Entity from "cesium/Source/DataSources/Entity";


const selectRegion = (event, viewer) =>  {

}

export default selectRegion;