/**
 * A really simple file for demonstrating a jest test
 * 
 */

/**
 * 
 * @param {Number} a 
 * @param {Number} b 
 * @returns 
 */
function sum(a, b)  {
    return a + b;
}

module.exports = sum;
